language: java
git:
  submodules: false

before_install:
  - cd ..
  - git clone --recurse-submodules git://jogamp.org/srv/scm/gluegen.git gluegen
  - pushd gluegen/make
  - ant
  - popd

install:
  - pushd jogl/make
  - ant
  - ant junit.run
